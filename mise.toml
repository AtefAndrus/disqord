[tools]
bun = "1.3"

[settings]
env_file = ".env"

[tasks.setup]
description = "Install dependencies and initialize environment file"
run = ["bun install", "if [ ! -f .env ]; then cp .env.example .env; fi"]
